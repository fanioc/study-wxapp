<!--pages/community/index.wxml-->
<!-- 顶部切换 -->
<view class="swiper-tab">
    <view class="swiper-tab-list swiper-left {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">动态圈</view>
    <view class="swiper-tab-list swiper-right {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">排行榜</view>
</view>
<view class="swiper-tab">

    <subject order-index='0' selected="{{currentTab}}" class="swiper-tab-list swiper-left" inner-text="动态圈" data-current="0" bindtap="swichNav"></subject>
      <subject  order-index='1' selected="{{currentTab}}" class="swiper-tab-list swiper-right " inner-text="动态圈" data-current="1" bindtap="swichNav" ><view>laj<view>hah</view></view></subject>
      
    
</view>

<!-- 置顶广告部分 -->


      <swiper indicator-dots="true"
        autoplay="true"   id='banner' class='banner'>
       <block wx:for="{{bannerList}}" wx:key="index">
       
          <swiper-item>
            <image mode="aspectFit" src="{{item.imgSrc}}" />
          </swiper-item>
        </block>

      </swiper>


<!-- 内容部分 -->
  <!-- 动态圈板块-->
<view id="dynamic" wx:if="{{currentTab == 0}}">
  动态圈板块
学生可以在这个板块发送：1，难题求解答的帮助，2，心情分享，感悟，
这个版块是否可以模仿成知乎的形式，只是问题的范围进行缩减，回答的用户角色进行限定（如仅限校内人员）
</view>

  <!-- 排行榜板块-->
<view id="rank"  wx:if="{{currentTab == 1}}">
排行榜板块
根据用户数据，在用户许可的情况下以排行榜的形式展示学生的学习情况。。平台甚至是用户可对排名靠前的学生进行打赏

</view>


  <theme inner="iew" >
  <text class="swiper-tab-list swiper-left">hah</text>
  </theme>